
module.exports = slice; 

/**
 * @description 搜索 
 * @param   { Array<V> } vblogs 
 * @param   { String   } idx 
 * @returns { Array<V> }
 */
function slice(vblogs, l, r){
    return vblogs.slice(l, r)
}

